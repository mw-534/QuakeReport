<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".EarthquakeActivity">
    <!-- because of the option showAsAction -> ifRoom
    and the fact that we only have one item, the menu
    won't appear but instead the MenuItem will be shown
    directly on the action bar.
    -->
    <item
        android:id="@+id/action_settings"
        android:title="@string/settings_title"
        android:icon="@mipmap/ic_filter"
        android:orderInCategory="1"
        app:showAsAction="ifRoom" />

</menu>